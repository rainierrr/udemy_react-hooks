version: '3'
services:
  node:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: node
    volumes:
      - ./:/usr/src/app 
    command: sh -c "cd react-hooks-101 && yarn start"
    ports:
      - "3000:3000"
    stdin_open: true
